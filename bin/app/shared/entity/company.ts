
export class Company {

  name: string;
  prefix: string;
  login_id: string;
  password: string;
  authentication_id: string;
  secret_key: string;
  ownership: string;
  isRepassword: boolean;
  isReCredential: boolean;
  isDeletable: boolean;
  // comment: string;

  // constructor(email: string, prefix: string, comment: string) {
  //    this.login_id = email;
  //    this.prefix = prefix;
  //    this.comment = comment;
  // }
}
